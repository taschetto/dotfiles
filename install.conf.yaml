- clean: ["~"]

- link:
    ~/.gitconfig:
      force: true
      path: git/gitconfig
    ~/.zshrc:
      force: true
      path: zsh/zshrc
    ~/.vimrc:
      force: true
      path: vim/vimrc

- shell:
    - [git submodule update --init --recursive, Installing submodules]
